[package]
name = "tracing-lv-track"
version = "0.1.0"
edition = "2024"

[features]

tokio = ["dep:tokio"]

[dependencies]
futures-util = { version = "0.3", default-features = false }
pin-project = "1"
tracing-core = "0.1"
tracing = "0.1"
tokio = { version = "1", default-features = false, optional = true }